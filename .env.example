# Bootstrap env for the Docker stack. SMTP, alert recipient i progi alertów
# konfigurujesz w panelu (/settings) po pierwszym zalogowaniu — nie tutaj.
#
# Wygeneruj APP_KEY:
#   docker compose run --rm --no-deps app php artisan key:generate --show

APP_ENV=production
APP_DEBUG=false
APP_URL=http://localhost:8080
APP_KEY=

# Set to true when serving over HTTPS — forces the Secure flag on session cookies.
# Leave empty for plain HTTP (local-only).
SESSION_SECURE_COOKIE=

# Port na hoście, pod którym wystawiony jest panel
NICEWATCH_HTTP_PORT=8080

# ---------- Pierwszy admin (auto-utworzony przy pierwszym `up`) ----------
# Konto seedowane jest idempotentnie — jeśli istnieje, seeder go zostawia.
#
# NICEWATCH_ADMIN_PASSWORD:
#   - puste lub 'CHANGE_ME' → seeder wygeneruje LOSOWE hasło i wypisze raz
#     w `docker compose logs app` (zalecane na pierwszy start)
#   - ustaw własne mocne hasło, żeby przejąć kontrolę bez czytania logów
#
# NICEWATCH_ADMIN_EMAIL puste = wyłącz auto-seedowanie i twórz konta
# wyłącznie przez `php artisan nicewatch:user:create`.
NICEWATCH_ADMIN_EMAIL=admin@nicewatch.local
NICEWATCH_ADMIN_PASSWORD=
NICEWATCH_ADMIN_NAME=Admin

# Opcjonalny dev mail catcher (Mailpit) — `docker compose --profile dev up -d`
MAILPIT_UI_PORT=8025
